# {{ api_name }}

## API Overview

**API ID**: `{{ api_id }}`
**Category**: {{ category }}
**HTTP Method**: {{ http_method }}
**URL**: `{{ url }}`
**Communication Type**: {{ communication_type }}

## Description

{{ api_name }} API for {{ category }}.

## TR_ID Information

| Environment | TR_ID |
|-------------|-------|
| Real Trading | `{% if real_tr_id %}{{ real_tr_id }}{% else %}N/A{% endif %}` |
| Paper Trading | `{% if paper_tr_id and paper_tr_id != "모의투자 미지원" %}{{ paper_tr_id }}{% else %}Not Supported{% endif %}` |

{% if real_tr_id == "" and paper_tr_id == "" %}**Note**: This API does not require a TR_ID.
{% elif paper_tr_id == "모의투자 미지원" %}**Note**: Paper trading is not supported for this API.
{% endif %}

## Endpoint

```
{{ http_method }} {{ url }}
```

## Request Headers

| Header | Value | Required | Description |
|--------|-------|----------|-------------|
| `Authorization` | `Bearer {access_token}` | Yes | OAuth 2.0 access token |
| `appkey` | `{app_key}` | Yes | KIS OpenAPI app key |
| `appsecret` | `{app_secret}` | Yes | KIS OpenAPI app secret |
| `tr_id` | `{% if real_tr_id %}{{ real_tr_id }}{% else %}N/A{% endif %}` | Conditional | TR_ID for real trading |
| `tr_id` | `{% if paper_tr_id and paper_tr_id != "모의투자 미지원" %}{{ paper_tr_id }}{% else %}N/A{% endif %}` | Conditional | TR_ID for paper trading |
| `custtype` | `P` or `B` | Yes | Customer type (P: Individual, B: Corporate) |

{% if http_method in ["GET", "POST"] %}

## Request Parameters

{% if api_id.startswith("국내주식") or api_id.startswith("해외주식") or api_id.startswith("v1_국내주식") %}
### Required Parameters

| Parameter | Type | Description | Required |
|-----------|------|-------------|----------|
| `tr_id` | string | TR_ID (Real: `{% if real_tr_id %}{{ real_tr_id }}{% endif %}`, Paper: `{% if paper_tr_id %}{{ paper_tr_id }}{% endif %}`) | Yes |
| `CANO` | string | Account number (8 digits) | Conditional |
| `ACNT_PRDT_CD` | string | Account product code (2 digits) | Conditional |
| `ETC_TR_ID_PREFIX` | string | TR_ID prefix for multiple TR_IDs | Conditional |

### Optional Parameters

For detailed parameter information, refer to the official KIS OpenAPI documentation.

{% elif api_id.startswith("인증") or api_id == "Hashkey" or api_id.startswith("실시간-") %}
### Required Parameters

| Parameter | Type | Description | Required |
|-----------|------|-------------|----------|
| `grant_type` | string | Grant type for OAuth 2.0 | Conditional |
| `code` | string | Authorization code | Conditional |
| `client_id` | string | OAuth 2.0 client ID | Conditional |
| `client_secret` | string | OAuth 2.0 client secret | Conditional |

### Optional Parameters

For detailed parameter information, refer to the official KIS OpenAPI documentation.

{% else %}
### Required Parameters

| Parameter | Type | Description | Required |
|-----------|------|-------------|----------|
| `tr_id` | string | TR_ID (Real: `{% if real_tr_id %}{{ real_tr_id }}{% endif %}`, Paper: `{% if paper_tr_id %}{{ paper_tr_id }}{% endif %}`) | Yes |

### Optional Parameters

For detailed parameter information, refer to the official KIS OpenAPI documentation.

{% endif %}
{% else %}
No parameters required beyond standard authentication headers.
{% endif %}

## Response

{% if communication_type == "REST" %}

### Response Format (REST)

JSON response format with standard KIS OpenAPI structure.

```json
{
  "rt_cd": "0",
  "msg_cd": "",
  "msg1": "Normal processing completed",
  "output": {
    // Response data fields
  }
}
```

### Response Fields

For detailed field information, refer to the official KIS OpenAPI documentation.

{% elif communication_type == "WEBSOCKET" %}

### Response Format (WebSocket)

WebSocket real-time data format.

```json
{
  "tr_id": "{{ real_tr_id }}",
  "rt_cd": "0",
  "msg_cd": "",
  "msg1": "",
  "output": {
    // Real-time data fields
  }
}
```

### Response Fields

For detailed field information, refer to the official KIS OpenAPI documentation.

{% endif %}

## Error Handling

### Error Response Format

```json
{
  "rt_cd": "1",
  "msg_cd": "EGW00223",
  "msg1": "Error message description"
}
```

### Common Error Codes

| Error Code | Description | Action |
|------------|-------------|--------|
| `EGW00123` | Invalid access token | Refresh access token |
| `EGW00223` | Invalid request parameters | Check request parameters |
| `EGW00331` | Rate limit exceeded | Implement retry with exponential backoff |
| `EGW00415` | Unauthorized access | Check authentication credentials |

## Example Usage

### Python Example

```python
import requests
from typing import Dict, Any

# Configuration
APP_KEY = "your_app_key"
APP_SECRET = "your_app_secret"
ACCESS_TOKEN = "your_access_token"
BASE_URL = "https://api.koreainvestment.com"
{% if paper_tr_id and paper_tr_id != "모의투자 미지원" %}
# Paper Trading
TR_ID = "{{ paper_tr_id }}"
{% elif real_tr_id %}
# Real Trading
TR_ID = "{{ real_tr_id }}"
{% else %}
# No TR_ID required for this API
TR_ID = None
{% endif %}

def make_api_request() -> Dict[str, Any]:
    """
    Make API request to {{ api_name }}.

    Returns:
        Dict[str, Any]: API response data
    """
    url = f"{BASE_URL}{{ url }}"

    headers = {
        "Authorization": f"Bearer {ACCESS_TOKEN}",
        "appkey": APP_KEY,
        "appsecret": APP_SECRET,
        "custtype": "P",  # P: Individual, B: Corporate
    }

    {% if real_tr_id or paper_tr_id %}
    # Add TR_ID if required
    if TR_ID:
        headers["tr_id"] = TR_ID
    {% endif %}

    {% if http_method == "GET" %}
    params = {
        # Add required parameters here
        # Example: "CANO": "12345678",
        #          "ACNT_PRDT_CD": "01",
    }

    response = requests.get(url, headers=headers, params=params)

    {% elif http_method == "POST" %}
    data = {
        # Add required parameters here
    }

    response = requests.post(url, headers=headers, json=data)

    {% else %}
    response = requests.post(url, headers=headers)

    {% endif %}
    response.raise_for_status()
    return response.json()

# Example usage
if __name__ == "__main__":
    try:
        result = make_api_request()

        if result.get("rt_cd") == "0":
            print("API call successful")
            print(f"Response: {result.get('output')}")
        else:
            print(f"API error: {result.get('msg1')}")
            print(f"Error code: {result.get('msg_cd')}")

    except requests.exceptions.RequestException as e:
        print(f"Request failed: {e}")
```

### Using KISRestClient (Recommended)

```python
from stock_manager.adapters.broker.kis import KISConfig, KISRestClient

# Configure client
config = KISConfig(
    kis_app_key="your_app_key",
    kis_app_secret="your_app_secret",
)

client = KISRestClient(config)

# Get TR_ID automatically
tr_id = client.get_tr_id(
    api_id="{{ api_id }}",
    is_paper_trading=False  # Set to True for paper trading
)

# Make request
response = client.make_request(
    method="{{ http_method.lower() }}",
    url="{{ url }}",
    params={...}  # Request parameters
)
```

## Notes

- This API requires valid OAuth 2.0 authentication credentials (app_key, app_secret, access_token)
- TR_ID must be set according to the trading environment (real or paper)
- For detailed parameter specifications and response fields, refer to the [official KIS OpenAPI documentation](https://apiportal.koreainvestment.com/)
- Rate limiting: Implement exponential backoff retry logic for rate limit errors
- Token refresh: Access tokens expire periodically; implement automatic token refresh

## Related APIs

{% if related_apis %}
{% for related_api in related_apis %}
- [{{ related_api.name }}](../{{ related_api.category_slug }}/{{ related_api.id }}.md) - `{{ related_api.api_id }}`
{% endfor %}
{% endif %}

## Category

[Back to {{ category }}](../index.md) | [API Documentation Index](../../index.md)
